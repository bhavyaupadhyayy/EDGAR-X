# Dashboard-only dependencies for Streamlit Community Cloud.
# Deliberately minimal — the deployed app reads a static snapshot and never
# imports the project's ingestion / ML / Anthropic / Snowflake stack.
streamlit>=1.40
pandas>=2.0
pyarrow>=15
plotly>=5.20
